{
    id: SERIAL PRIMARY KEY,
    flavor: VARCHAR(256),
    img_url: VARCHAR(256),
    type: VARCHAR(256),
    dairy_free: BOOLEAN,
    vegan: BOOLEAN,
    will_licks: INTEGER,
    logan_licks: INTEGER
}

